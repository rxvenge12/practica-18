<Window x:Class="practica_18.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="250" Width="350">
    <Grid Margin="0,10,0,-10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Margin="20,20,20,20">
            <Label Content="Логин
                   " FontSize="16" FontWeight="Bold"/>
            <TextBox x:Name="LoginTextBox" Margin="0 0 0 10"/>
            <Label Content="Пароль
                   " FontSize="16" FontWeight="Bold"/>
            <PasswordBox x:Name="PasswordBox" Margin="0 0 0 10"/>
            <Button Content="Вход" Click="LoginButton_Click"/>
            <TextBlock x:Name="ErrorMessageTextBlock" Text="" Foreground="Red" Margin="0 10 0 0"/>
        </StackPanel>
    </Grid>
</Window>
